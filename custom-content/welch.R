# -----------------------------------------------------------------------------
#   MANUEL WELCH
# -----------------------------------------------------------------------------

# variable (x er n)
s1 <- 467.9^2
s2 <- 409^2
x <- 50

# t står for tæller og n1 og n2 står for de to dele nævneren
t <- ((s1/x) + (s2/x))^2
n1 <- ((s1/x)^2) / (x - 1)
n2 <- ((s2/x)^2) / (x - 1)

t / (n1 + n2)
